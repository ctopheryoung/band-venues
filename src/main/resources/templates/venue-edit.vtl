<h2>$venue.getName()</h2>

<h4>Rename $venue.getName():</h4>

<form action="/venues/$venue.getId()" method="post">
  <div class="form-group">
    <input required value="$venue.getName()" type="text" class="form-control" name="name" id="name">
  </div>
  <button type="submit" class="btn btn-info">Save</button>
</form>
