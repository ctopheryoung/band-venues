<h2>$band.getName()</h2>

<h4>Rename $band.getName():</h4>

<form action="/bands/$band.getId()" method="post">
  <div class="form-group">
    <input required value="$band.getName()" type="text" class="form-control" name="name" id="name">
  </div>
  <button type="submit" class="btn btn-info">Save</button>
</form>
